import React from 'react'

const Readmore = ({ className }) => {
  return (
    <div>
      <button className={`${className} border border-white text-white font-bold py-2 px-4 rounded`}>Read More</button>

    </div>
  )
}

export default Readmore
